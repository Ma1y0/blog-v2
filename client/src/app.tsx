import { useState } from 'preact/hooks'
import Router from "preact-router"
import './app.css'

export function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      <Router>
        
      </Router>
    </>
  )
}
